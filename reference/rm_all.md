# Remove all objects from global environment

Same as `rm(list=ls())`

## Usage

``` r
rm_all()
```

## Value

No return value, called for side effects

## Examples

``` r
if (FALSE) { # \dontrun{
rm_all()
} # }
```
